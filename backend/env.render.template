## Render için ENV örneği (kopyalayıp Render dashboard'da Environment sekmesine gir)
## NOT: Bu dosyayı git'e commit edebilirsin, secret içermez.

NODE_ENV=production

# MongoDB Atlas (zorunlu)
MONGO_URI=mongodb+srv://USER:PASS@CLUSTER.mongodb.net/DBNAME?retryWrites=true&w=majority

# JWT (zorunlu)
JWT_SECRET=super-strong-secret
JWT_REFRESH_SECRET=super-strong-refresh-secret
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Frontend (Vercel) domainin
FRONTEND_URL=https://YOUR-PROJECT.vercel.app

# CORS (prod + preview domainler varsa virgülle)
CORS_ORIGINS=https://YOUR-PROJECT.vercel.app


